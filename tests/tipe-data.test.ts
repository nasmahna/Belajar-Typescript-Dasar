describe('Data Type', function () {
    it('should declare', function() {

        const name: string  = "Nasmah Nur Amiroh";
        const balance: number = 2000000;
        const isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);

    });
});